# manage-agent-memory

Add/edit/delete pin and semantic rows for an agent (human source).

**API:** `openAddPinDialog`, `openEditPinDialog`, `confirmDeletePin`, `openAddSemanticDialog`, `openEditSemanticDialog`, `confirmDeleteSemantic`, drafts.

**Server:** `application/memory` pin/semantic use-cases, `adapters/http/memory`.
