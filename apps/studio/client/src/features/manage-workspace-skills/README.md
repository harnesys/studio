# manage-workspace-skills

Создание skill-файла в workspace.

**API:** `openCreateSkillDialog`, `CreateSkillDialog`.

**Server:** `application/workspaces` (`*-skill*`), `adapters/skills-fs.adapter.ts`.
