{
  "name": "harnesys",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*",
    "apps/studio/*"
  ],
  "scripts": {
    "build:host": "bun build --compile apps/studio/server/src/index.ts --outfile harnesys-host",
    "docs:merge": "bun run scripts/docs-merge.ts",
    "vendor:plugin-schemas": "bun run scripts/vendor-plugin-schemas.ts",
    "lint": "biome check .",
    "lint:fix": "biome check --write --unsafe .",
    "typecheck": "bun run --cwd packages/harnesys typecheck && bun run --cwd apps/studio typecheck"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.6",
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
