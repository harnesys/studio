Раунд 2: воспроизведу оба бага и покрываю инструменты, которые в прошлый раз не трогал (`load_tools`, параметры `offset/limit` и `replace_all`, `grep` по скрытым путям, `memory scope=session`, `schedule_pause`, `threadId=self`, HEAD/POST в fetch).



Bag reproducer подтверждён: `webhook_delete` → снова FK. Плюс `list_dir(".harnesys")` вернул **пусто**, хотя `shell ls` видит `mcp.json`, `skills/`, `threads/` — скрытые пути фильтруются на уровне list_dir. Продолжаю:



Баг №2 воспроизведён: `nextRunAt` снова равен моменту создания вместо 1 янв 04:00 — и этот cron нацелен на **этот** чат, так что могу получить ложное срабатывание. Ставлю на паузу немедленно:



Пауза сработала (`status: paused`) — ложного пробуждения не будет. `replace_all` дал 3 замены, HEAD/POST прошли (405 от example.com — это ожидаемо, метод-пластинг работает), `read_file` скрытый путь читает нормально. Зачистка + тест `recall_save` и `schedule_delete`:



Раунд 2 завершён, план закрыт. Он же **исправил два вывода раунда 1** — честно говорю, где я был неправ.

## Что подтвердилось и изменилось

| Инструмент | Р1 | Р2 | Вердикт |
|---|---|---|---|
| `read_file` offset/limit | — | окно из 2 строк, `truncated: true`, `totalLines: 5` | ✅ |
| `edit_file replace_all` | — | 3 замены за один проход | ✅ |
| `fetch` HEAD / POST | GET 200 | HEAD 200 (пустое тело), POST 405 | ✅ методы доходят |
| `load_tools` | — | `web_search`/`screenshot`/`web_run` → `unknown`, `loaded: []` | ⚠️ deferred-каталог пуст |
| `memory` scope=session | — | write→list→delete чисто | ✅ |
| `schedule_pause` | — | `status: paused` | ✅ |
| `schedule_delete` | ❌ «not found» | ✅ `ok: true` | ✅ **я был неправ в Р1** |
| `schedule_set` nextRunAt | ⚠️ | ⚠️ снова `nextRunAt == createdAt` | 🐛 воспроизведено 2× |
| `webhook_delete` | ❌ FK | ❌ FK | 🐛 воспроизведено 2× |
| `recall_save` | — | ❌ `tool not found recall_save` | 🐛 новый |
| `list_dir(".harnesys")` | — | пусто, хотя `ls` видит `mcp.json`, `skills/`, `threads/` | ⚠️ скрытые пути |

**Важная поправка к Р1.** `schedule_delete` полностью рабочий. В первом раунде он не нашёл запись, потому что schedule **успел сработать и самоудалиться** из-за бага с `nextRunAt` (cron на 1 января вы numerовался «прямо сейчас»). То есть это один баг, а не два, и он опаснее, чем казалось: любой новый cron с `mode: "dont_ask"` выполнится немедленно. Обходной путь — сразу `schedule_pause` после `schedule_set` (проверено).

**Находка про MCP.** `.harnesys/mcp.json` объявляет `duckduckgo-mcp-server` и `playwright`, но `load_tools` не видит ни одного deferred-инструмента → серверы не подняты. Похоже, их надо запускать (`uvx` / `npx`), это отдельная задача.

## Состояние workspace после зачистки
Workdir чист: `.toolcheck2/` удалён, `git status` пустой, schedules `[]`, session-память пуста. Осталось намеренно: 2 long-memory записи о багах. Осадок: `toolcheck-webhook` (paused) + его тред — удалить нельзя из-за FK-бага.

Не проверено, и я не буду трогать без твоего «да»: `agents_create` (нет `agents_delete` — создам мусор в workspace), `agents_handoff` (переключит этот тред на другого агента), `ask_user` (ты его сейчас видишь в работе).



Все три. Начинаю с диагностики MCP + создаю временного агента (одним батчем), потом cron-эксперимент, handoff оставлю напоследок — он переключит оратора в этом треде.



`agents_create` упал с внятной ошибкой: **`cycle_budget: graph has cycle but no budget.maxSteps/deadlineMs`** — дефолтный ReAct-граф цикличный, значит `budget` обязателен, если не передаёшь свой `graph`. Повторяю с бюджетом; заодно probe имён deferred-инструментов и создаю cron на 06:29 (сейчас 06:26):



Третий раз тот же паттерн: `nextRunAt = createdAt = 06:26:23` прикроне `29 6 10 09 *`. Баг стабильный. Пробую `budget.policy: "error"` и заодно сплю 45 с, чтобы увидеть факт срабатывания раньше 06:29:



Две новые находки: `agents_create` с невалидным бюджетом **всё равно занял имя** (`agent name taken` — частично записался, хотя вернул ошибку), и `shell` имеет дефолтный таймаут 30 с (мой `sleep 45` его убил). Проверяю:



Ключевое: расписание **не выстрелило сразу** — в 06:28:05 `lastFiredAt: null`, а `nextRunAt` завис в прошлом (06:26:23). Значит мой вывод из Р1 был неверен в обе стороны. И ещё: `toolcheck-echo` **создан** (id `7832cf13`), несмотря на две ошибки валидации — откат не отработал. Проверяю историю `.studio/` и жду границы 06:29:



Однозначно: сейчас 06:30:03, граница крона 06:29:00 **пройдена**, а `lastFiredAt: null`, `fires: []`. То есть расписание не сработало вообще — ни немедленно, ни по границе. Значит вывод Р1 («выстрелило и самоудалилось») был неверен. Осталось разделить два случая — dedicated-тред против `threadId=self`:



Теперь истина вскрылась, и она хуже. Dedicated-schedule **выстрелил в 06:31:00.714** при кроне `33 6 10 09 *` (граница была 06:33) — то есть сразу при создании, а не по расписанию. А `threadId=self` не выстрелил вообще. И `nextRunAt` после fired уехал в `2027-09-10T03:33Z` — плюс год и минус 3 часа (TZ-сдвиг). Убираю за собой:



Всё зачищено: `schedule_list: []`, workdir = `.gitignore` + `.harnesys/mcp.json` + skills, `git status` чистый.

## 1. MCP — не подняты, причину нашёл

- `.harnesys/mcp.json` на месте, бинарники есть: `uvx → /usr/local/bin/uvx`, `npx → node v24.13.0`
- но `load_tools` не знает **ни одного** deferred-инструмента — прострелял 7 имён (`web_search`, `screenshot`, `web_run`, `duckduckgo_search`, `playwright_navigate`, `browser_navigate`, `mcp__playwright__browser_navigate`) → `loaded: []`, все в `unknown`

Вывод: хост не запускает MCP-серверы из `.harnesys/mcp.json`. Это след миграции — в git-истории конфиг лежал в `.studio/mcp.json`, а скиллы в `.agents/skills/`; коммит `e5cb6b4 "update"` их удалил. Из агента не чинится — нужен рестарт workspace (или проверить, какой путь хост читает сейчас).

Побочная находка: `knowledge_search` индексирует **удалённые** `.studio/*.md` — тот «прошлый отчёт по тестам», который я нашёл в раунде 1, это мусор от несуществующих файлов. Индекс устарел.

## 2. Cron — довели до конца, я был неправ дважды

| Вариант | Факт |
|---|---|
| dedicated thread, `33 6 10 09 *` | **выстрелил в 06:31:00**, граница была 06:33 → сразу при создании |
| `threadId=self`, `29 6 10 09 *` | граница пройдена (06:30), `lastFiredAt: null` → **никогда не сработает** |
| `nextRunAt` при создании | всегда `= createdAt` (4/4 случая) |
| `nextRunAt` после fired | `2027-09-10T03:33Z` — **+1 год и −3 часа**, TZ-skew |
| `schedule_delete` | работает; «not found» в Р1 был следствием авто-файра (здесь я был неправ, что заподозрил delete) |
| `schedule_peek` | `fires: []` при `history: "none"` — по дизайну, но неочевидно; для чтения нужнен `history: "last"` |

**Практически:** любой cron с `mode: "dont_ask"` выполнится в момент создания — это ломает «ежедневный джоб», но удобно как одноразовый запуск. Пробудить **этот** чат по расписанию нельзя, только отдельный schedule-тред.

## 3. Агенты

- `agents_create` валидирует бюджет **после** вставки имени: две ошибки подряд (`cycle_budget` → `budget.policy`), а агент создан: `toolcheck-echo`, id `7832cf13-9c2e-4dd1-af86-9273b8a7fda0`. `agents_delete` в тулсете нет — удалять через UI.
- Если не передаёшь `graph`, `budget` **обязателен**, а `budget.policy` принимает только `"ask"` / `"error"`.
- `tools: []` в `agents_list` при двух объявленных — но у Assistant тоже `[]`, так что это особенность листинга, не баг.

Итог покрытия: **все 32 инструмента тронуты**, из них сломаны 4 (`webhook_delete` FK, `recall_save` → «tool not found», немедленный файр крона, мёртвый `threadId=self`) + MCP-каталог пуст.

Последнее — `agents_handoff`. Сейчас оратора переключит на `toolcheck-echo`; он по инструкции вернёт тред обратно мне. Если не вернёт — переключись руками на Assistant.

