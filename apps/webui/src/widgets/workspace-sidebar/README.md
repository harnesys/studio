# workspace-sidebar

Header: strip of 5 slots (WORKSPACE_TAB_CAP) — workspace avatars on desk (shift/control — multi-select), ghosts of off-desk workspaces (click — return to desk), empty slots with "+" (click — create), ⋯ menu. Sections: Inbox, agents, files, schedules, webhooks, create workspace.

**Depends on:** `features/desk`, `create-workspace`, `manage-agent`, `manage-schedule`, `open-file`.
