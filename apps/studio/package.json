{
  "name": "@harnesys/studio",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run dev.ts",
    "dev:server": "bun run --cwd server dev",
    "dev:ui": "bun run --cwd client dev",
    "build": "bun run --cwd client build",
    "start": "bun run --cwd server start",
    "db:generate": "bun run --cwd server db:generate",
    "db:migrate": "bun run --cwd server db:migrate",
    "db:studio": "bun run --cwd server db:studio",
    "typecheck": "bun run --cwd shared typecheck && bun run --cwd server typecheck && bun run --cwd client typecheck",
    "lint": "bun run --cwd shared lint && bun run --cwd server lint && bun run --cwd client lint",
    "lint:fix": "bun run --cwd shared lint:fix && bun run --cwd server lint:fix && bun run --cwd client lint:fix"
  }
}
