# Баг-репорт: Studio чат и run lifecycle

Обновлён: 2026-09-06. Стенд: `main` @ `fbd5c50`, API 3000, Vite 5173, агент Jarvis (qwen3.8-flash, BAI), dev-БД `~/.harnesys/studio.db`. Открыт пункт 1, раздел «Исправлено» — справка.

## 1. Очередь параллельных `ask_user` — фикс есть, live-проверка не доведена

Код: `9ee1717` — `ask_user` назначает `interruptId = ask/{toolCallId}`, resume отдаётся только вызову с совпавшим id, остальные переспрашивают (очередь вместо broadcast «first-wins по показу, ответ всем троим» — тест-лог хозяина `.studio/ask-user-test.md`).

Проверка не выполнена: тест сорвался из-за п. 2 (ран ушёл в отравленный тред). Сценарий: свежий тред → попросить агента вызвать `ask_user` три раза параллельно → ответить на первый вопрос → ожидание: показываются вопросы 2 и 3 по очереди, каждый получает свой ответ, в ленте три разных результата.

Сопутствующие UX-замечания из того же тест-лога (не блокеры, отдельные решения): нет Cancel — пустой ответ недостижим, отказ приходится выражать текстом; `id` варианта не возвращается агенту; разделитель ` · ` перегружен (склейка multi-выбора и свободного текста); дубликаты `label` неоднозначны.

## Исправлено (2026-09-04)

- Осколки текста после завершения/reload, сырой markdown, дубли плиток пользователя, зависание композера «Agent is thinking…» — коалесценция дельт в проекции, идентитет события по `clientEventId`, `user.message` пишется один раз, feed закрывается по терминальному статусу (`8f5b26a` и предшествующие, детали в git log).
- Permission loop на параллельных вызовах (бесконечное чередование `act:0/0 ↔ act:0/1`): grants и completed-результаты живут в чекпоинте батча (`9ee1717`).
- Лента: единый каркас `ActivityLine` — один рельс, одинаковые иконки/цвета/отступы, collapse/expand, Ask разворачивается (`09cb891`).
