# Studio API (Bun / Hono)
PORT=3000

# Vite dev server
VITE_DEV_SERVER_PORT=5173

# Optional: override data root (default: ~/.harnyx)
# HARNYX_HOME=

# Diagnostics (console [harnyx:trace])
SERVER_TRACE=false
CLIENT_TRACE=false

