# workspace-sidebar

Шапка: табы-аватары воркспейсов (до 4, shift/control — мультиселект) + меню ⋯. Секции: Inbox, агенты, files, schedules, webhooks, create workspace.

**Зависит от:** `features/desk`, `create-workspace`, `manage-agent`, `manage-schedule`, `open-file`.
