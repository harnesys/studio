# git-commit

Commit / Push / Update (pull) for workspace-git. Dialog for Commit, direct calls for Push/Pull.

**API:** `openCommitDialog(message?)`, `GitCommitDialog`.

**Server:** `CommitGit`, `PushGit`, `PullGit` + `GitCliAdapter`.
