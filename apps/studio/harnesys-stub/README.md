# harnesys-stub

Временный пакет: Studio компилируется, пока нет `packages/harnesys` ≥0.3.

- Chat/runtime: no-op
- Не копировать имена, типы, API и подходы в библиотеку
- SoT библиотеки: `docs/01`–`docs/22`, `docs/ROADMAP.md`
- Studio обязана уйти на нативные типы docs; после cutover stub убрать

См. также `apps/studio/AGENTS.md` (блок про stub) и ROADMAP «Правило Studio».
