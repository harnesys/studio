# chat-transcript

Лента треда: turns, tool detail, activity rail.

**API:** `ChatTranscript`.  
**Зависит от:** `features/desk`, `features/send-message`, `entities/journal`.  
**Parse tool output:** `model/tool-output.ts` (оркестратор) + `tool-detail-types`, `tool-json`, `tool-output-diff|file|shell`.
