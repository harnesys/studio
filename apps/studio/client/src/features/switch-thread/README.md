# switch-thread

Новый тред, branch, close, confirm delete.

**API:** `openNewThread`, `branchThread`, `closeThread`, `threadById`, `confirmDeleteThread`.

**Server:** `application/threads` (`create/update/delete`).
